# PROGRESS

## いま何をやっているか
**🗣 長文が早口になる不具合を直した（2026-08-25）**。`/synthesis` も110字で切って合成するようにし、
version 0.3.2 で**サーバは再起動済み**（127.0.0.1:3952 で応答確認済み）。コミット `f1f1409` は push 済み。

- 原因は**尺のクランプ**。`inference_runtime` が duration predictor の予測フレーム数を
  `max_frames = max_seconds(=30.0) * sr / hop_length` で頭打ちにするので、30秒を超える文章を
  丸ごと投げると**30秒の枠に押し込むぶん早口**になる
- `/render` は `split_script(text, 110)` で切っていたが、`/synthesis` は `_synth_tensor` へ
  丸投げで分割を通っていなかった。**econte が叩くのは `/synthesis`**
- 実測（econteの仮ナレ・ナレーション話者）: 256字の1行が**きっかり30.0秒＝8.5字/秒**
  （他カットは約4.2字/秒）の2倍速 → 分割後は **51.5秒＝5.0字/秒** で正常に戻った
- 直し: `_synth_chunked` を足して110字超は句読点で切って合成し繋ぐ。チャンク間は **150ms**
  （段落用のレンダー350msより詰める。`pause_ms` で上書き可）。分割したときはログに残す。
  モノラル/48kHzへの揃えは `_to_native` に切り出してレンダー側と共通化

**この直しは econte だけやなく say.sh や他のクライアントにも効く**（サーバ側で閉じた修正）。

## 次にやること
- [ ] econte 側で C1（256字）と C5（163字）を「録り直し」して、実際の音を耳で確認する
      （econte は行単位でキャッシュするので、原稿が同じ限り古いテイクを使い回す）
- [ ] 150ms のチャンク間無音が自然か、長文を何本か聞いて調整する
- [ ] レンダー（/render）側の 350ms と、区切り110字も合わせて聞き直す
- [ ] サーバを launchd 常駐にするか検討（今はアプリ右上の「▶ サーバを起動」で都度立ち上げ）
- [ ] リポジトリ・ダッシュボードの 🔌ポートタブで 3950 / 3951 / 3952 を予約登録する
- [ ] 🤖 原稿AI をアプリ経由で押して Codex が返すか確認（CLI単体では確認済み）
- [ ] ⋯メニューの seed 引き継ぎが「似た声のまま口調だけ変わる」か耳で確かめる（理屈上は保証なし）
- [ ] `DESIGN_REF_TEXT`（参照クリップ用の原稿・約100文字→20.9秒）をもっと声の特徴が出る文に育てるか検討
- [ ] 絵文字による感情制御を試して、どの絵文字がどう効くか手元メモを作る

## ハマっている点・未解決事項
- **30秒の壁はモデル側の仕様**。分割で回避しとるだけで、1チャンクが30秒を超えたらまた早口になる。
  読点も句点も無い超長文は `split_script` がそのまま1チャンクで通すので、そこは今も無防備
- チャンクを跨ぐと**声の調子が微妙に変わる可能性**がある（チャンクごとに合成しているため）。
  seed は文章から決まるので再現性はあるが、繋ぎ目の自然さは耳で確認が要る
- econte 側のキャッシュは行の文章が変わらん限り効くので、サーバを直しても**録り直さんと古い音のまま**
