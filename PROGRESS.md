# PROGRESS

## いま何をやっているか
**🍡 声カードの ⋯（ミートボール）メニュー ＋ 📋 プロンプト表示・作り直しを実装した**（2026-08-25）。
デザインした声の caption（作ったときの説明文）を後から読めて、そこに「もっと甘えた声で」みたいな
追記をして**別の声として**作り直せるようになった。元の声は消えへん。

- `app/src/index.html` だけの変更（サーバ・API は一切いじってない。`GET /speakers` が
  もともと `designed / caption / seed` を返してるのでフロントだけで完結）
- カード右上に `⋯`。中身は **📋 プロンプトを見る / 🎨 このプロンプトを元に新しい声を作る /
  🎧 この声を選ぶ / 🗑 この声を削除**（削除はカードの 🗑 からここへ引っ越し）
- プロンプト画面: caption を編集できるテキストエリア＋**追記チップ8種**（＋もっと甘えた声／
  もっと落ち着いて／もっと元気に／もっと低い声／もっと高い声／やさしく丁寧に／ささやくように／
  関西のイントネーション）＋ seed 表示＋📋コピー＋「元の声の seed を引き継ぐ」チェック
- 🎨 を押すと声デザイン画面が **caption 入り・名前は「フロント女子2」** の状態で開く。
  seed を引き継いだときは 🎲 のラベルが「元の声の seed でこの説明を試す」に変わる
- 音声ファイルから作った声（たかたか本人など）は caption が無いので、その旨を出すだけにしてる

検証済み（ヘッドレスブラウザで実操作・console error 0件）:
- 6人ぶんのカードに ⋯ が出る／⋯ を押しても話者は切り替わらん／カード本体クリックは今まで通り選択
- ⋯ → プロンプト表示 → チップで追記 → seed引き継ぎON → 声デザイン画面に caption・名前・seed が入る
- 本人（protected）はメニューに削除が出ず、プロンプト画面は「プロンプトは残ってへん」表示
- Esc・外側クリック・同じ ⋯ の再クリックでメニューが閉じる
- 本番ビルドして `/Applications/いろとりスタジオ.app` を差し替え済み

**✏️ 名前の変更も足した**（2026-08-25 夕）。⋯メニューに「名前を変える」。
`PATCH /speakers/<id>` を server.py 0.3.1 に追加（表示名だけ差し替え・音声/latent は焼き直さない）。
ヘッドレスQA＋実サーバでのリネーム往復まで確認済み。サーバは再起動済み・アプリも差し替え済み。

## 次にやること
- [ ] 実機で ⋯ を押して、甘えた声バージョンを1人作ってみる（🎲→🔁→登録まで通す）
- [ ] seed 引き継ぎが実際に「似た声のまま口調だけ変わる」のか、耳で確かめる（理屈上は保証なし）
- [ ] 🤖 原稿AI を実際に押して、Codex が返してくるか確かめる（CLI単体では動作確認済み。アプリ経由は未確認）
- [ ] Claude に切り替えたときも通るか確かめる（環境を削ると "Not logged in" になったのでPATH補強を入れてある）
- [ ] ぷれぜん君で🔄して、作った6人が仮ナレの声リストに出るか確かめる
- [ ] `DESIGN_REF_TEXT`（参照クリップ用の原稿・約100文字→20.9秒）を、もっと声の特徴が出る文に育てるか検討
- [ ] レンダー済み音声を聞いて、チャンクの区切り（110文字）と無音 350ms が自然か調整
- [ ] サーバを launchd 常駐にするか検討（今はアプリ右上の「▶ サーバを起動」で都度立ち上げ）
- [ ] リポジトリ・ダッシュボードの 🔌ポートタブで 3950 / 3951 / 3952 を予約登録する
- [ ] 絵文字による感情制御を実際に試してどの絵文字がどう効くか手元メモを作る

## ハマっている点・未解決事項
- **ポップアップの「外側クリックで閉じる」を `setTimeout(…,0)` で張ると取りこぼす**。
  実測で登録が数百ms遅れて Esc が効かへんケースが出た。開いたときの mousedown はもう終わってるから、
  `mousedown` の監視は**同期で即登録**してええ（対処済み）
- **seed は19桁**。JSON の数値で返すと JS の Number（2^53）で精度が落ちて別の声になる。
  文字列で返す・文字列のまま持つこと（対処済み。CONTEXT.md の罠に記載）
- デザインした声は「生成音を参照にした声」＝原理上コピーのコピー。silentcipher の透かしも乗っている。
  実用上は問題なさそうやけど、本人の声と比べて音質がどうかは実際に聞いて確かめたい
- caption だけでは声は決まらん（ガチャ）。狙った声を一発で出す方法は無い
- MPS では fp32 のみ。bf16 高速化は CUDA/XPU 専用なので Mac では効かない
- 1チャンク 15秒前後かかる（32ステップ）。長文は分数ぶんの待ちになる。急ぐなら `--num-steps` を下げる
