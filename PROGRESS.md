# PROGRESS

## いま何をやっているか
**いろとりスタジオ（app/）を作った**（2026-08-22）。声の元ファイルをドロップ→声データベース登録、原稿→48kHz WAV 保存。
- `server.py` 0.2.0: `POST/DELETE /speakers`・`POST /render`・`GET /jobs`・`/synthesis` の `caption`/`sample_rate` 追加。econte 互換は維持（curl で確認済み）
- API 検証済み: 話者追加 3秒 → 3クリップ＋latent、削除で後片付けまで確認。render で 48kHz/mono の wav 保存を確認
- Tauri アプリ `app/`（かべちゃん方式・Vite無し）。アイコン生成済み

それ以前（完了済み）: Mac ローカル運用（MPS）、たかたか本人の参照音声（ALAC 48kHz）、WebUI :3950/:3951、常駐サーバ :3952 で econte 連携

## 次にやること
- [ ] たかたかが実機でドロップ→登録→原稿→保存まで触って使用感を確認（声の名前、抑揚、演技メモ）
- [ ] レンダー済み音声をいくつか聞いて、チャンクの区切り（110文字）と無音 350ms が自然か調整
- [ ] サーバを launchd 常駐にするか検討（今はアプリ右上の「▶ サーバを起動」で都度立ち上げ）
- [ ] リポジトリ・ダッシュボードの 🔌ポートタブで 3950 / 3951 / 3952 を予約登録する
- [ ] 絵文字による感情制御を実際に試してどの絵文字がどう効くか手元メモを作る

## ハマっている点・未解決事項
- MPS では fp32 のみ。bf16 高速化は CUDA/XPU 専用なので Mac では効かない
- 1チャンク 15秒前後かかる（32ステップ）。長文は分数ぶんの待ちになる。急ぐなら `--num-steps` を下げる
