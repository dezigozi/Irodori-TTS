# PROGRESS

## いま何をやっているか
**🎨 声をデザインする（server.py 0.3.0 ＋ いろとりスタジオ）を実装して、econte（ぷれぜん君）まで通した**（2026-08-24）。
実在の人の声を1本も使わずに、caption（どんな声かの説明文）だけでバーチャル話者を作れるようになった。

- `server.py` 0.3.0: `POST /design`（試し聞き・`X-Irodori-Seed` を返す）／`POST /design/speakers`（話者登録ジョブ）
  ／`GET /version` に `voice_design`／`GET /speakers` に `designed・caption・seed`。**既存APIは変更なし**
- `app/src/index.html`: ドロップ欄の下に「🎨 声をデザインして作る（人の声いらず）」ボタン → モーダルで
  caption入力（プリセット6種のチップつき）→ 🎲試し聞き → 🔁もう一声（ガチャ）→ 名前つけて登録
- **econte 側は改修ゼロで動く**（`GET /speakers` を serde_json::Value で読んでるので追加フィールドは無害）。
  親切のため NarrationStudio の案内文に「いろとりスタジオで声を足したら 🔄 を押したら出てくる」を1行足しただけ
- 両方とも本番ビルドして /Applications に差し替え済み（いろとりスタジオ／ぷれぜん君）

検証済み（実測）:
- 参照音声なしで生成できる（`prepare_reference: 0.3ms`＝参照を読んでへん）。試し聞き1本 11〜15秒
- 登録ジョブ: 20.9秒の音声を1発生成 → 10秒×2本のクリップ＋latent → 話者として `POST /synthesis` できる
- **同じ caption + 同じ seed で WAV が md5 完全一致**（＝気に入った声を確実に呼び戻せる）
- seed を変えると同じ caption でも別人が出る（＝ガチャが効いてる）
- 検証で作った話者・refs のゴミは掃除済み（speakers.json は takataka のみ）

- **🤖 原稿AI と 絵文字ガイドを追加**（2026-08-24 午後）
  - ヘッダに「🤖 原稿AI」…ローカルCLIで ChatGPT(Codex) / Claude を叩くチャット。既定は **gpt-5.6-terra / effort low**。
    モデルとエフォートは画面で選べて設定に残る。返答の ``` ブロックは「📝原稿へ / 🎬演技メモへ / 🎨声デザインへ」で流し込める
  - ヘッダに「⋯」…絵文字45種の効果と例文の一覧。クリックで原稿のカーソル位置に挿し込む
  - Rust に `ask_ai` / `ai_available`。タイムアウト300秒→kill→2回リトライ、コマンドは絶対パス＋PATH補強
  - たかたかが声を6人（フロント女子・若手営業マン・若手メカ・ナレーション・先輩メカ(関西)）作成済み

## 次にやること
- [ ] 🤖 原稿AI を実際に押して、Codex が返してくるか確かめる（CLI単体では動作確認済み。アプリ経由は未確認）
- [ ] Claude に切り替えたときも通るか確かめる（環境を削ると "Not logged in" になったのでPATH補強を入れてある）
- [ ] ぷれぜん君で🔄して、作った6人が仮ナレの声リストに出るか確かめる
- [ ] 気に入った声の seed を控えておく運用にするか決める（speakers.json には残ってるので消さん限り再現できる）
- [ ] `DESIGN_REF_TEXT`（参照クリップ用の原稿・約100文字→20.9秒）を、もっと声の特徴が出る文に育てるか検討
- [ ] レンダー済み音声を聞いて、チャンクの区切り（110文字）と無音 350ms が自然か調整
- [ ] サーバを launchd 常駐にするか検討（今はアプリ右上の「▶ サーバを起動」で都度立ち上げ）
- [ ] リポジトリ・ダッシュボードの 🔌ポートタブで 3950 / 3951 / 3952 を予約登録する
- [ ] 絵文字による感情制御を実際に試してどの絵文字がどう効くか手元メモを作る

## ハマっている点・未解決事項
- **seed は19桁**。JSON の数値で返すと JS の Number（2^53）で精度が落ちて別の声になる。
  文字列で返す・文字列のまま持つこと（対処済み。CONTEXT.md の罠に記載）
- デザインした声は「生成音を参照にした声」＝原理上コピーのコピー。silentcipher の透かしも乗っている。
  実用上は問題なさそうやけど、本人の声と比べて音質がどうかは実際に聞いて確かめたい
- caption だけでは声は決まらん（ガチャ）。狙った声を一発で出す方法は無い
- MPS では fp32 のみ。bf16 高速化は CUDA/XPU 専用なので Mac では効かない
- 1チャンク 15秒前後かかる（32ステップ）。長文は分数ぶんの待ちになる。急ぐなら `--num-steps` を下げる
